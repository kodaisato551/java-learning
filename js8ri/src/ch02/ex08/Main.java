package ch02.ex08;

import java.util.stream.Stream;

public class Main {
    /**
     * 最初のストリームと次のストリームから交互に要素を取り出し、
     * どちらかのストリームから要素がなくなったら停止する
     * TODO tbd
     *
     * @param first
     * @param second
     * @param <T>
     * @return
     */
    public static <T> Stream<T> zip(Stream<T> first, Stream<T> second) {
        return null
    }
}
